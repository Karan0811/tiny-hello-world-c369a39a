# Cheat Sheet

- Build: `python -m build`
- Install: `pip install .`
- Editable: `pip install -e .`
- Upload: `twine upload dist/*`
- GitHub install: `pip install git+https://github.com/user/repo.git`
